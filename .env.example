# FLAMBOARD Configuration
# For production on Railway: leave NEXT_PUBLIC_SOCKET_URL empty or unset.
# The client will automatically connect to window.location.origin (the public Railway domain).
# Set this ONLY if your Socket.IO server is hosted on a completely separate domain.
NEXT_PUBLIC_SOCKET_URL=
